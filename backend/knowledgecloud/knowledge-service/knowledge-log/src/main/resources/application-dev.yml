#服务器端口
server:
  port: 8103

#数据源配置
spring:
  datasource:
    url: ${blade.datasource.dev.url}
    username: ${blade.datasource.dev.username}
    password: ${blade.datasource.dev.password}

# 日志服务自身不再把日志写回自己（ILogClient 指向的就是本服务，写日志失败会自调用死循环）
knowledge:
  log:
    write-enabled: false
