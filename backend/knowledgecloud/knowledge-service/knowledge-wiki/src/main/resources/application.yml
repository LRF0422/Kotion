knowledge:
  xss:
    # Page documents are structured JSON and must be stored byte-for-byte.
    # Context-specific escaping or sanitization belongs at render boundaries.
    skip-url:
      - /page/*/ops
      - /page/*/reconcile
