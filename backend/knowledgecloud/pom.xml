<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.knowledge</groupId>
    <artifactId>Knowledge</artifactId>
    <version>3.4.1</version>
    <packaging>pom</packaging>

    <properties>
        <knowledge.tool.version>3.4.1</knowledge.tool.version>
        <knowledge.project.version>3.4.1</knowledge.project.version>

        <java.version>1.8</java.version>
        <maven.plugin.version>3.8.0</maven.plugin.version>
        <swagger.version>2.10.5</swagger.version>
        <swagger.models.version>1.6.2</swagger.models.version>
        <knife4j.version>2.0.9</knife4j.version>
        <protostuff.version>1.6.0</protostuff.version>
        <captcha.version>1.6.2</captcha.version>
        <easyexcel.version>2.2.6</easyexcel.version>
        <mica.auto.version>1.2.5</mica.auto.version>
        <alibaba.cloud.version>2021.0.1.0</alibaba.cloud.version>
        <alibaba.nacos.version>2.1.0</alibaba.nacos.version>
        <spring.boot.admin.version>2.7.1</spring.boot.admin.version>
        <spring.plugin.version>2.0.0.RELEASE</spring.plugin.version>
        <org.mapstruct.version>1.5.2.Final</org.mapstruct.version>
        <hutool-all.version>5.8.18</hutool-all.version>
        <flyway.version>9.22.3</flyway.version>
        <mysql.connector.version>8.0.33</mysql.connector.version>

        <spring.boot.version>2.7.1</spring.boot.version>
        <spring.cloud.version>2021.0.3</spring.cloud.version>
        <spring.platform.version>Cairo-SR8</spring.platform.version>
        <!-- 1.18.24 predates JDK 21+; 1.18.38 supports JDK 25 annotation processing -->
        <lombok.version>1.18.38</lombok.version>

        <!-- 推荐使用Harbor -->
        <docker.registry.url>192.168.3.43</docker.registry.url>
        <docker.registry.host>${docker.registry.url}:5000</docker.registry.host>
        <docker.host>http://127.0.0.1:2375</docker.host>
        <docker.plugin.version>1.0.0</docker.plugin.version>
    </properties>

    <modules>
        <module>knowledge-auth</module>
        <module>knowledge-gateway</module>
        <module>knowledge-ops</module>
        <module>knowledge-service</module>
        <module>knowledge-service-api</module>
        <module>knowledge-common</module>
        <module>knowledge-tool</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.plugin</groupId>
                <artifactId>spring-plugin-core</artifactId>
                <version>${spring.plugin.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.plugin</groupId>
                <artifactId>spring-plugin-metadata</artifactId>
                <version>${spring.plugin.version}</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring.cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>io.spring.platform</groupId>
                <artifactId>platform-bom</artifactId>
                <version>${spring.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- Override nimbus-jose-jwt version from platform-bom (5.1) to support Spring Security 5.7+ -->
            <dependency>
                <groupId>com.nimbusds</groupId>
                <artifactId>nimbus-jose-jwt</artifactId>
                <version>9.22</version>
            </dependency>
            <dependency>
                <groupId>org.mapstruct</groupId>
                <artifactId>mapstruct</artifactId>
                <version>${org.mapstruct.version}</version>
            </dependency>
            <dependency>
                <groupId>cn.hutool</groupId>
                <artifactId>hutool-all</artifactId>
                <version>${hutool-all.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>jitpack.io</id>
            <url>https://jitpack.io</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.name}</finalName>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
            <resource>
                <directory>src/main/java</directory>
                <includes>
                    <include>**/*.xml</include>
                </includes>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring.boot.version}</version>
                    <configuration>
                        <finalName>${project.build.finalName}</finalName>
                    </configuration>
                    <executions>
                        <execution>
                            <goals>
                                <goal>repackage</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${maven.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <encoding>UTF-8</encoding>
                    <compilerArgs>
                        <arg>-parameters</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.mapstruct</groupId>
                            <artifactId>mapstruct-processor</artifactId>
                            <version>${org.mapstruct.version}</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok-mapstruct-binding</artifactId>
                            <version>0.2.0</version>
                        </path>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                        <!-- other annotation processors -->
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!--
        Database migrations are intentionally opt-in and are never bound to a
        Maven lifecycle or a Spring Boot service startup. Invoke Flyway goals
        explicitly with -Pdb-migrate from this aggregator project.
    -->
    <profiles>
        <profile>
            <id>db-migrate</id>
            <properties>
                <flyway.url>${env.FLYWAY_URL}</flyway.url>
                <flyway.user>${env.FLYWAY_USER}</flyway.user>
                <flyway.password>${env.FLYWAY_PASSWORD}</flyway.password>
                <flyway.locations>filesystem:${maven.multiModuleProjectDirectory}/script/migration</flyway.locations>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.flywaydb</groupId>
                        <artifactId>flyway-maven-plugin</artifactId>
                        <version>${flyway.version}</version>
                        <inherited>false</inherited>
                        <configuration>
                            <url>${flyway.url}</url>
                            <user>${flyway.user}</user>
                            <password>${flyway.password}</password>
                            <locations>
                                <location>${flyway.locations}</location>
                            </locations>
                            <baselineOnMigrate>false</baselineOnMigrate>
                            <cleanDisabled>true</cleanDisabled>
                            <connectRetries>0</connectRetries>
                            <outOfOrder>false</outOfOrder>
                            <validateOnMigrate>true</validateOnMigrate>
                            <validateMigrationNaming>true</validateMigrationNaming>
                            <failOnMissingLocations>true</failOnMissingLocations>
                        </configuration>
                        <dependencies>
                            <dependency>
                                <groupId>org.flywaydb</groupId>
                                <artifactId>flyway-mysql</artifactId>
                                <version>${flyway.version}</version>
                            </dependency>
                            <dependency>
                                <groupId>com.mysql</groupId>
                                <artifactId>mysql-connector-j</artifactId>
                                <version>${mysql.connector.version}</version>
                            </dependency>
                        </dependencies>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <distributionManagement>
        <repository>
            <id>knowledge-release</id>
            <url>http://14.29.235.4:8888/repository/knowledge-release/</url>
        </repository>

        <snapshotRepository>
            <id>knowledge-snapshot</id>
            <url>http://14.29.235.4:8888/repository/knowledge-snapshot/</url>
        </snapshotRepository>
    </distributionManagement>

</project>
