import { ExtensionWrapper } from "@kn/common";
import { UnknownNode } from "./unknown-node";


export const UnknownNodeExtension: ExtensionWrapper = {
    extendsion: UnknownNode,
    name: UnknownNode.name,
}